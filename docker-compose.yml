services:
  backend:
    image: ghcr.io/moizahmad08/samsonpos-backend:latest
    container_name: sansons_backend
    restart: always
    environment:
      - DEBUG=${DEBUG:-False}
      - SECRET_KEY=${SECRET_KEY:-django-insecure-production-default-key-please-change-in-env}
      - ALLOWED_HOSTS=${ALLOWED_HOSTS:-*}
      - DB_ENGINE=${DB_ENGINE:-django.db.backends.postgresql}
      - DB_NAME=${DB_NAME:-postgres}
      - DB_USER=${DB_USER:-postgres}
      - DB_PASSWORD=${DB_PASSWORD:-your_supabase_password_here}
      - DB_HOST=host.docker.internal
      - DB_PORT=5435
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "6000:8000"

  frontend:
    image: ghcr.io/moizahmad08/samsonpos-frontend:latest
    container_name: sansons_frontend
    restart: always
    ports:
      - "4000:80"
    depends_on:
      - backend
